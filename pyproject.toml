[tool.commitizen]
name = "cz_conventional_commits"
version = "0.6.0"
version_files = ["pyproject.toml", "version.json:version"]
tag_format = "v$version"
version_scheme = "semver2"
changelog_incremental = true
